import type { TileType } from "../core/components/Tile"

//Put all component details / specs here
export const spec:TileType[] = [
    {
        color: "pink",
        title: "Tab Selector",
        description: "A Tab Selector component that easily allows for new tabs and tab functions to be added.",
        link: "/comp-test"
    }
]